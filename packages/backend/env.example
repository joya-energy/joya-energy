# =========================
# JOYA BACKEND ENVIRONMENT VARIABLES
# =========================

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/joya
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/joya?retryWrites=true&w=majority

# Email Configuration (SMTP)
# -------------------------------------
# For local development, use one of the options below:

# OPTION 1: Gmail (for testing - NOT recommended for production)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# EMAIL_FROM=your-email@gmail.com
# EMAIL_TO=recipient@example.com

# OPTION 2: Mailtrap (recommended for development - catches all emails)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your-mailtrap-username
# SMTP_PASS=your-mailtrap-password
# EMAIL_FROM=noreply@joya.com
# EMAIL_TO=admin@joya.com

# OPTION 3: Ethereal (automatic test account - no signup needed)
# Run: npm run setup-email to generate credentials
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=generated-user@ethereal.email
# SMTP_PASS=generated-password
# EMAIL_FROM=noreply@joya.com
# EMAIL_TO=admin@joya.com

# OPTION 4: Production Email Services
# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=your-mailgun-smtp-username
# SMTP_PASS=your-mailgun-smtp-password

# AWS SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Logging
LOG_LEVEL=debug

# External Integrations
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
GOOGLE_MAPS_API_URL=https://maps.googleapis.com/maps/api/geocode/json
NASA_POWER_API_URL=https://power.larc.nasa.gov/api/temporal/daily/point
NASA_POWER_COMMUNITY=RE
NASA_POWER_PARAMETER=ALLSKY_SFC_SW_DWN
ENERGY_COST_PER_KWH=0.35
SOLAR_COST_PER_KWP=3000

# Audit énergétique coefficients
ENERGY_AUDIT_K_CH=0.3
ENERGY_AUDIT_K_FR=0.2
ENERGY_AUDIT_ECS_GAS_EFF=0.92
ENERGY_AUDIT_ECS_SOLAR_COVERAGE=0.7
ENERGY_AUDIT_ECS_SOLAR_APPOINT_EFF=0.92
ENERGY_AUDIT_ECS_PAC_COP=3

# AI Services (Bill Extraction)
OPENAI_API_KEY=sk-proj-...