{
  "buildCommand": "npm install --prefix ../.. && node scripts/generate-env.js && node ../../node_modules/@angular/cli/bin/ng.js build --configuration production",
  "outputDirectory": "dist/joya-frontend/browser",
  "installCommand": "echo 'Skipping install, will run from buildCommand'",
  "rewrites": [{ "source": "/((?!.*\\.).*$)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
